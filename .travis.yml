---
dist: xenial
sudo: required
language: python
python: "3.7"
branches:
  only:
    - master

env:
  - VERSION: 5
  - VERSION: 6
  - VERSION: 7

services:
  - docker

install:
  - pip install ansible molecule docker

script:
  - molecule test

notifications:
  webhooks: https://galaxy.ansible.com/api/v1/notifications/